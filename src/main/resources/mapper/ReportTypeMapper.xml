<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fusion.docfusion.mapper.ReportTypeMapper">

    <resultMap id="ReportTypeResultMap" type="com.fusion.docfusion.entity.ReportType">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.fusion.docfusion.entity.ReportType"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO report_type (name, description, created_at)
        VALUES (#{name}, #{description}, #{createdAt})
    </insert>

    <update id="update" parameterType="com.fusion.docfusion.entity.ReportType">
        UPDATE report_type
        SET name        = #{name},
            description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE
        FROM report_type
        WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="ReportTypeResultMap">
        SELECT *
        FROM report_type
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="ReportTypeResultMap">
        SELECT *
        FROM report_type
        ORDER BY created_at DESC
    </select>
</mapper>

