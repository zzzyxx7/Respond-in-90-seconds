# 与 docker-compose.dev.yml 中 RabbitMQ 默认账号一致（dev 已默认 guest，本 profile 可选）。
# 使用方式：spring.profiles.active=dev,docker（在 dev 之后加载，可再次固定 doc.rabbitmq）
doc:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
